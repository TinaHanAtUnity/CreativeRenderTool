sudo: false

language: python

addons:
  apt:
    packages:
    - libimage-exiftool-perl

install:
  - . $HOME/.nvm/nvm.sh
  - nvm install stable
  - nvm use stable
  - pip install awscli
  - npm install

cache:
  directories:
    - node_modules

script: make lint && make test-coverage && make test-integration && make build-test && make travis-browser-test && make build-release && make deploy

notifications:
  slack:
    rooms:
      - unity:PLPHkrYyAyDCW5umUmcUDIZZ
    on_success: change
    on_failure: always
    on_start: never
