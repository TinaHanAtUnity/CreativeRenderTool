/************************
* GENERAL
*************************/
@import "vars.styl";

html {
  width: 100%;
  height: 100%;
  background-color: transparent;
  font-size: $default-px-size;
}

body.iphone,
body.ipad,
body.android {
  margin: 0;
  padding: 0;
  width: 100%;
  height: 100%;
  background-color: transparent;
  color: $black;
  font-family: Helvetica, sans-serif;
  -webkit-text-size-adjust: none;
  -webkit-touch-callout: none;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
  -webkit-user-select: none; // @stylint ignore
  -webkit-user-drag: none; // @stylint ignore
}


@import "overlay.styl";
@import "closable-video-overlay.styl";
@import "new-video-overlay.styl";
@import "end-screen.styl";
@import "icons.styl";
@import "mraid.styl";
@import "moat.styl";
@import "display-interstitial.styl";
@import "playable-mraid.styl";
@import "media-queries.styl";
@import "vpaid.styl";
@import "loading.styl";
@import "closer.styl";
@import "admob.styl";
@import "promo.styl";
@import "vast-end-screen.styl";
@import "privacy.styl";



// zSlider styles
.z-slide-wrap {
    position: relative;
    display: block;
    min-height: 100%;
    min-width: 100%
}

.z-slide-content {
    position: relative;
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    list-style: none;
    overflow: hidden;
}

.z-slide-item {
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    transform: translate3d(0, 0, 0);

    &>span {
        position: relative;
        display: block;
        width: 100%;
        height: 100%;
        text-decoration: none;

        background-repeat: no-repeat;
        -webkit-background-size: cover;
        -moz-background-size: cover;
        background-size: cover;
        background-position: center;
    }
    img {
        position: absolute;
        left: 0;
        top: 0;
        right: 0;
        bottom: 0;
        max-width: 100%;
        max-height: 100%;
        margin: auto;
    }
}

$dotColor = #fff;
$dotActiveColor= #099fde;
$dotRadius = 0.25rem;

.z-slide-indicator {
    position: absolute;
    bottom: 0.5rem;
    left: 0;
    z-index: 10;
    .z-slide-dot {
        display: block;
        float: left;
        width: $dotRadius * 2;
        height: $dotRadius * 2;
        margin-left: 0.2rem;
        margin-right: 0.2rem;
        border-radius: 50%;
        background-color: $dotColor;

        &.active {
            background-color: $dotActiveColor;
        }
    }
}