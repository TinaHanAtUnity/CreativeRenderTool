@import "vars.styl";

$accessible-mraid-close-button-color = #b1b1b1;

#extended-mraid .accessible-close-button {

    .icon-close {
        color:$accessible-mraid-close-button-color;
        text-shadow: none;
        z-index: 5;
    }

    .circle-base {
        background-color: $white;
    }
}