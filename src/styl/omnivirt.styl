.omnivirt-container {
  position: relative;
  display: inline-block;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

.omnivirt-placeholder {
  position: absolute;
  width: 100%;
  height: 100%;
}
