transitionMixinAnimationExperiment(position) {
    transition: position .8s cubic-bezier(.215, .61, .355, 1); // easeInOutQuad
}

$dark-cover-animation-experiment = #0c292f;

.animated-video-overlay-experiment {
    #end-screen {
        &.on-show {
            overflow: hidden;
        }
        .campaign-container {
            background: none;
        }
        .campaign-container:before {
            position: absolute;
            width: 100%;
            height: 100%;
            background: $dark-cover-animation-experiment;
            content: "";
            opacity: 0;
            transitionMixinAnimationExperiment(opacity);
        }
    }
    #end-screen.on-show-animation  .campaign-container:before {
        content: "";
        opacity: 1;
    }

    &.portrait :not(.on-show)#end-screen {
        .game-background-container {
            top: -50%;
            transitionMixinAnimationExperiment(top);
        }

        &.on-show-animation .game-background-container {
            top: 0;
        }

        .end-screen-info-background {
            bottom: -50%;
            transitionMixinAnimationExperiment(bottom);
        }

        &.on-show-animation .end-screen-info-background {
            bottom: 0;
        }
    }

    $.landscape :not(.on-show)#end-screen {
        .game-background-container {
            left: -50%;
            transitionMixinAnimationExperiment(left);
        }

        &.on-show-animation .game-background-container {
            left: 0;
        }

        .end-screen-info-background {
            right: -50%;
            transitionMixinAnimationExperiment(right);
        }

        &.on-show-animation .end-screen-info-background {
            right: 0;
        }
    }
}
